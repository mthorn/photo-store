!!! 5
%html{ 'ng-app' => (controller_name == 'main' && 'photo-store') }
  %head
    %title Photo Store
    %meta{ name: 'viewport', content: 'width=device-width, initial-scale=1' }
    = stylesheet_link_tag 'application', media: 'all'
    %script#config{ 'data-config' => render(file: 'application/_config.json.jbuilder') }
    = javascript_include_tag 'application'
    = csrf_meta_tags

  %body= yield
